workflow:
  steps:
    - trigger_services:
        project: home:Aazam:feilong
        packages: # Use "packages" (plural) and a list
          - zthin_deb
          - zthin_rhel
          - zthin_sles
          - zvmsdk_deb
          - zvmsdk_rhel
          - zvmsdk_sles
    - rebuild_package:
        project: home:Aazam:feilong
        packages: # Use "packages" (plural) and a list
          - zthin_deb
          - zthin_rhel
          - zthin_sles
          - zvmsdk_deb
          - zvmsdk_rhel
          - zvmsdk_sles
  filters:
    branches:
      only:
        - master
  event: push
